import 'package:flutter/material.dart';

class ArabicScreen extends StatelessWidget {
  const ArabicScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Arabic Screen'),
      ),
    );
  }
}
